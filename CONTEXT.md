# Route Mock

使用者在 Android 手機上設定位置或步行路線，讓其他接受模擬定位的 App 收到指定的位置變化。

## Language

**模擬定位（Mock location）**：由使用者指定的位置訊號，保留其模擬性質；接收端可以辨識並選擇是否接受。
_Avoid_: 真實 GPS、隱形定位

**路線（Route）**：從起點經過有序途經點到終點的完整行走軌跡。
_Avoid_: 起終點直線、目的地

**途經點（Waypoint）**：使用者要求路線經過的位置。規劃結果可能將它對齊附近可步行路網。
_Avoid_: 軌跡取樣點

**真實起點（Real origin）**：由目前真實定位建立的第 1 個途經點；新的行走路線從這裡開始，其後為使用者選擇的目的地。
_Avoid_: 預設台北、任意遠方起點

**定位（Locate）**：取得目前真實位置並置中地圖；模擬中按下此操作即同意結束模擬、完成清理後再定位。
_Avoid_: 暫停、在 mock 樣本上判定真實位置

**行走模式（Playback mode）**：開始行走前選擇的路線重複方式，包括單次、原路往返與閉合循環。
_Avoid_: 隨機行走、速度模式

**單次（Once）**：沿路線從第一點走到最後一點，抵達後維持定點。

**原路往返（Ping-pong）**：沿確認的路線行走，到最後一點後循同一條路反向返回；回到第一點再往前行走，持續往返。
_Avoid_: 重新規劃返程、瞬移回起點

**閉合循環（Loop）**：沿包含最後一點返回第一點之步行路段的完整閉合路線，持續重複行走。
_Avoid_: 直線補接、走完跳回起點

**路線位置（Route position）**：目前位置距離這份路線起點的沿線距離；返程時減少，閉合循環換圈時歸零。
_Avoid_: 累計里程、累計步數

**模擬行程（Session）**：從開始輸出位置，到使用者恢復真實定位或發生中斷的一次活動；停止行走後仍持續輸出定點。
_Avoid_: 背景工作、遊戲行程

**定點（Hold）**：持續輸出同一位置，移動速度為零的行程。

**暫停（Pause）**：保留行程進度並定點；繼續後從原進度行走。
_Avoid_: 停止

**停止行走（Stop walking）**：結束路線移動，持續輸出最後採樣位置與零速度；不能用「繼續」重啟這次移動。
_Avoid_: 關閉模擬定位、恢復真實位置

**恢復真實定位（Restore real location）**：結束模擬行程並釋放定位輸出。接收端可能跳回手機的真實位置；何時取得新的真實位置由裝置與接收端決定。
_Avoid_: 暫停、保證立即取得真實 GPS

**到達（Arrived）**：單次模式已抵達終點並在終點定點，直到恢復真實定位；往返與循環會繼續行走。

**跨 App 驗證（Cross-app verification）**：由獨立安裝的接收 App 確認模擬位置、時間與移動速度。
_Avoid_: 自己地圖上的藍點、遊戲相容性

**遊戲相容性（Game compatibility）**：特定遊戲版本在特定裝置上接受定位並能正常遊玩的結果；不能由跨 App 驗證推定。
